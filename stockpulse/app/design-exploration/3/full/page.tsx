import type { Metadata } from 'next'
import LandingNavy from '@/components/design-exploration/full/LandingNavy'

export const metadata: Metadata = { title: 'Full landing page — Exploration 3 (preview)' }

export default function DesignExploration3FullPage() {
  return <LandingNavy />
}
