import type { Metadata } from 'next'
import LandingNavy from '@/components/landing/LandingNavy'

export const metadata: Metadata = { title: 'Full landing page — Exploration 3 (now live at /)' }

export default function DesignExploration3FullPage() {
  return <LandingNavy />
}
