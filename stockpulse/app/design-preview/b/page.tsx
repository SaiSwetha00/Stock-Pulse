import type { Metadata } from 'next'
import { DirectionB } from '@/components/design-preview/Directions'

export const metadata: Metadata = { title: 'Direction B — Bold modern SaaS' }

export default function DesignDirectionB() {
  return <DirectionB />
}
