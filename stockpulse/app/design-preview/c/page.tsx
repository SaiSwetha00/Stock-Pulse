import type { Metadata } from 'next'
import { DirectionC } from '@/components/design-preview/Directions'

export const metadata: Metadata = { title: 'Direction C — Premium editorial' }

export default function DesignDirectionC() {
  return <DirectionC />
}
