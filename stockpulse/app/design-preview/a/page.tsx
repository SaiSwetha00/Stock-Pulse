import type { Metadata } from 'next'
import { DirectionA } from '@/components/design-preview/Directions'

export const metadata: Metadata = { title: 'Direction A — Light minimal product' }

export default function DesignDirectionA() {
  return <DirectionA />
}
