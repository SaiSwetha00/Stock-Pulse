import PageSkeleton from '@/components/ui/PageSkeleton'

/** The schedule carries a right-hand availability rail. */
export default function StaffLoading() {
  return <PageSkeleton stats={0} rows={5} sidePanel />
}
